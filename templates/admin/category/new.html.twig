{% extends 'base.html.twig' %}

{% set action = app.current_route == 'app_admin_category_new' ? 'Ajout' : 'Édition' %}

{% block title %}{{ action }} de categorie{% endblock %}

{% block body %}
<!-- Header-->
<section class="container px-4">
    <div class="w-50 m-auto">
        <h1>{{ action }} de categorie</h1>

        {{ form_start(form) }}
        {{ form_row(form.name, {label: 'Nom'}) }}
        {{ form_row(form.color, {label: 'Category'}) }}
        <button type="submit" class="btn btn-primary">Envoyer</button>
        {{ form_end(form) }}
    </div>
</section>
{% endblock %}
