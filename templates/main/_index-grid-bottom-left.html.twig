<div >
    <ul>
   {# {% for depense in depenses %}
        <li>{{depense.category.name }} : {{ depense.amount}}</li>
        <li>
            {{ component('ProgressBarDepense', { percentage: depense.amount*100/total }) }}
        </li>
    {% endfor %}#}
    </ul>
</div>