<div class="card mb-1">
    <div class="card-body">
        <div class="card-title d-flex justify-content-between">
            <h4 class="mb-1">{{ depense.category.name }}</h4>
            <small class="text-muted">Valeur : {{ depense.amount }}</small>
        </div>
        <div class="card-title d-flex justify-content-between">
            <h5 class="mb-1">{{ depense.created |date('d M Y')}}</h5>
        </div>
    </div>
</div>